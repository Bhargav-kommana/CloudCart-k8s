apiVersion: v1
kind: Service

metadata:
  name: cloudcart-frontend
  namespace: cloudcart

spec:
  selector:
    app: cloudcart-frontend

  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000

  type: LoadBalancer